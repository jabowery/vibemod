[pytest]
addopts = -q --maxfail=1 --disable-warnings --cov-report=term-missing:skip-covered --cov-report=html
testpaths = tests
python_files = test_*.py

# Ensure the project root is importable
pythonpath = .
