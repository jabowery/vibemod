[pytest]
addopts = -q --maxfail=1 --disable-warnings --cov=modify_code --cov-report=term-missing:skip-covered --cov-report=html
testpaths = tests_code_mod
python_files = test_*.py

# Ensure the project root is importable
pythonpath = .
